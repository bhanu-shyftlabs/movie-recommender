// Vercel serverless function stub — replace with real API routes (one file per route).
export default function handler(req: any, res: any) {
  res.status(200).json({ message: 'Hello from Vercel Node' });
}
