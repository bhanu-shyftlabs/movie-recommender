import React from 'react';
import { createRoot } from 'react-dom/client';
createRoot(document.getElementById('root')).render(<><h1>movie-recommender</h1><p>Full POC — React/Vite frontend + Express proxy backend in one repo, one Docker image</p></>);
